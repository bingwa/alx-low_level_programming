#include "main.h"

/**
 * _abs - compute the absolute value of an integer
 * @n : the integer to check
 *
 * Return: the absolute value of int
 */

int _abs(int n)

{
		return (n >= 0 ? n : n * -1);
}

